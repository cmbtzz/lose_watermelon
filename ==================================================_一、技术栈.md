你是一名资深全栈 Web 开发工程师。请帮我创建一个可部署到 Linux 云服务器上的 AI 聊天网页。

项目目标：

我要制作一个类似 ChatGPT 的私人/小规模 AI 聊天网页。

我的服务器上可能已经运行 AstrBot，也可能没有。因此，在编写 AstrBot 对接代码之前，必须先判断当前环境中是否存在 AstrBot、AstrBot 是否正在运行，以及 AstrBot 当前版本实际提供什么 API。

严禁根据记忆直接猜测 AstrBot 的 API 路径、请求参数、鉴权方式、SSE 格式或 WebSocket 格式。

==================================================
一、技术栈
==================================================

前端：

- Vue 3
- Vite
- TypeScript
- Composition API
- Element Plus
- markdown-it
- highlight.js

网络请求：

- 普通 HTTP 请求使用 fetch 或 axios
- AI 流式回复优先使用 SSE
- 如果实际 AstrBot API 使用 WebSocket，则根据实际接口采用 WebSocket

服务器：

- Linux
- Nginx
- HTTPS 最终通过 Let's Encrypt / Certbot 配置

AI 后端：

优先使用已经存在的 AstrBot。

AstrBot 后面已经配置或可能配置 Gemini API，因此网页本身不要保存 Gemini API Key，也不要让浏览器直接访问 Gemini。

==================================================
二、第一阶段：检测 AstrBot
==================================================

在写任何聊天接口代码之前，首先检查当前机器环境。

依次检查：

1. 是否存在 AstrBot 目录或相关文件。

例如可以检查：

- 当前项目附近目录
- /opt
- /srv
- /home
- Docker 容器
- Docker Compose 项目

但不要无意义地遍历整个文件系统。

2. 检查 Docker：

运行类似：

docker ps
docker compose ps

查找名称、镜像或命令中包含：

astrbot
AstrBot

的服务。

3. 检查进程：

检查是否存在 AstrBot Python 进程或相关服务。

4. 检查监听端口：

使用：

ss -lntp

或等价命令。

判断 AstrBot 当前监听在哪个 IP 和端口。

5. 如果发现 AstrBot：

不要立刻假设 API。

进一步检查：

- AstrBot 版本
- AstrBot 配置文件
- 本机安装代码
- 官方文档
- OpenAPI / Swagger
- 已注册路由

优先以“当前安装版本”的实际代码和接口定义为准。

6. 尝试寻找：

/openapi.json
/docs
/swagger
/api

等 API 描述入口。

但只能进行合理探测，不进行破坏性操作。

7. 最终输出 AstrBot 检测结果：

例如：

AstrBot detected: yes
Version: xxx
Address: 127.0.0.1:xxxx
Chat API: xxx
Streaming: SSE / WebSocket / unsupported
Authentication: xxx

如果没有发现，则明确输出：

AstrBot detected: no

不要伪造接口。

==================================================
三、AstrBot 不存在时
==================================================

如果没有检测到 AstrBot：

不要自动下载安装 AstrBot。

不要擅自安装任何大型依赖。

先完成前端和 ChatService 抽象。

创建：

src/services/chat/types.ts
src/services/chat/chatService.ts
src/services/chat/astrbotAdapter.ts
src/services/chat/mockAdapter.ts

定义统一接口，例如：

interface ChatMessage {
    id: string
    role: 'user' | 'assistant' | 'system'
    content: string
    createdAt: number
}

interface ChatRequest {
    conversationId: string
    message: string
}

interface ChatService {
    sendMessage(
        request: ChatRequest,
        callbacks: {
            onChunk: (text: string) => void
            onComplete: () => void
            onError: (error: Error) => void
        }
    ): Promise<void>
}

如果 AstrBot 不存在：

使用 MockChatService 让前端可以正常运行和调试。

同时在 README 中说明：

“当前未检测到 AstrBot，需要配置 AstrBot 后才能启用真实 AI。”

==================================================
四、AstrBot 存在时
==================================================

如果 AstrBot 存在：

创建 AstrBotAdapter。

但是 AstrBotAdapter 必须完全依据实际检测到的 AstrBot API 编写。

禁止出现：

“假设 AstrBot API 是 /api/chat”

这种代码。

需要确认：

- HTTP Method
- URL
- Header
- Token / API Key
- Body JSON
- conversation/session 参数
- SSE 数据格式
- 完成事件
- 错误事件
- 文件上传格式

然后将 AstrBot 的实际协议转换成项目内部统一 ChatService。

前端永远只调用：

ChatService

不要直接：

fetch("http://127.0.0.1:xxxx/astrbot-specific-api")

这样以后可以替换后端。

==================================================
五、网页功能
==================================================

制作一个现代 AI Chat UI。

整体布局类似：

+-----------------------------------------+
| Sidebar      | Chat                     |
|              |                          |
| New Chat     | messages                 |
| Chat 1       |                          |
| Chat 2       |                          |
|              |                          |
| Settings     |                          |
|              | input box                |
+-----------------------------------------+

必须实现：

1. 左侧会话栏

包含：

- 新建会话
- 会话历史列表
- 当前选中会话
- 删除会话

第一版历史记录可以存在 localStorage。

设计时要方便以后替换成服务器数据库。

2. 消息区域

支持：

- user 消息
- assistant 消息
- Markdown
- 标题
- 无序列表
- 有序列表
- 表格
- 引用
- inline code
- code block
- syntax highlighting

3. AI 流式输出

AI 回答不能等完整生成后才一次性显示。

必须逐步显示 token / chunk。

显示效果类似 ChatGPT。

4. 自动滚动

AI 回复过程中：

如果用户当前位于页面底部，则自动跟随回复滚动。

如果用户主动向上翻看历史，则不要强制拉回底部。

5. 输入框

支持：

Enter：
发送消息

Shift + Enter：
换行

发送期间：

显示停止生成按钮。

6. 停止生成

使用：

AbortController

或对应 WebSocket 关闭方式。

7. 状态

需要显示：

Generating...

连接失败

请求失败

429 / rate limit

服务器错误

AstrBot unavailable

不要出现无限 loading。

==================================================
六、UI 风格
==================================================

风格：

简洁
现代
偏 ChatGPT / Claude 风格
不要做成传统后台管理系统

默认：

深色模式

但设计为未来可以切换：

light
dark

页面必须适配：

PC
平板
手机

移动端左侧 Sidebar 自动变成 Drawer。

聊天主体最大宽度控制在合理范围，例如：

800px - 1000px

不要让文字在超宽显示器上铺满屏幕。

==================================================
七、安全要求
==================================================

非常重要。

浏览器不能：

直接访问 Gemini API

不能：

把 Gemini API Key 放在：

.env 前端变量
JavaScript
Vue 文件
localStorage

如果 AstrBot 有 API Token：

也尽可能不要暴露给浏览器。

推荐结构：

Browser
↓
Nginx
↓
必要时 Thin Backend / Proxy
↓
AstrBot
↓
Gemini

如果 AstrBot API 本身不适合直接暴露浏览器：

创建一个非常薄的代理服务。

可以使用：

FastAPI

但只有确实需要时才添加。

不要为了“架构漂亮”无意义增加后端。

==================================================
八、Nginx
==================================================

生成一份：

deploy/nginx.conf.example

要求：

1. Vue SPA

支持：

try_files $uri $uri/ /index.html;

2. /api/

反代到 AstrBot 或代理层。

3. 不暴露 AstrBot 的管理后台。

4. AstrBot 应尽可能只监听：

127.0.0.1

而不是：

0.0.0.0

5. API 限流

配置合理的：

limit_req_zone

例如针对聊天接口进行单 IP 限流。

不要设置得过度严格。

6. 请求体大小限制

例如：

client_max_body_size

防止超大请求。

7. 合理 timeout

AI 流式请求可能持续较久，因此：

proxy_read_timeout

不能设置得太短。

8. 如果使用 SSE：

确认 Nginx 不会缓存流式响应。

必要时配置：

proxy_buffering off;

==================================================
九、服务器安全
==================================================

README 中给出部署建议。

云服务器安全组只开放必要端口：

80
443

SSH：

22

如果方便，建议 SSH 只允许可信 IP。

以下端口不能直接公网开放：

AstrBot
MySQL 3306
Redis 6379
Docker API 2375
内部 FastAPI
其他数据库

架构应该类似：

Internet
↓
80 / 443
↓
Nginx
↓
127.0.0.1:内部服务

==================================================
十、Gemini 免费 API
==================================================

Gemini API 可能是免费额度，因此会遇到：

429
rate limit
quota exceeded

前端必须友好处理。

例如：

“当前模型请求过于频繁，请稍后重试。”

不要无限重试。

最多允许非常有限的退避重试，或者直接交给用户重新发送。

Nginx 自身也应该进行简单限流，避免有人疯狂调用接口占满 Gemini 免费额度。

==================================================
十一、目录结构
==================================================

推荐生成：

ai-chat/
├── src/
│   ├── components/
│   │   ├── ChatSidebar.vue
│   │   ├── ChatMessage.vue
│   │   ├── ChatInput.vue
│   │   └── MarkdownRenderer.vue
│   │
│   ├── views/
│   │   └── ChatView.vue
│   │
│   ├── services/
│   │   └── chat/
│   │       ├── types.ts
│   │       ├── chatService.ts
│   │       ├── astrbotAdapter.ts
│   │       └── mockAdapter.ts
│   │
│   ├── stores/
│   │   └── conversations.ts
│   │
│   ├── App.vue
│   └── main.ts
│
├── deploy/
│   ├── nginx.conf.example
│   └── README.md
│
├── .env.example
├── package.json
├── vite.config.ts
└── README.md

如果实际项目需要额外后端：

backend/
├── main.py
├── astrbot_proxy.py
└── requirements.txt

否则不要创建 backend。

==================================================
十二、配置
==================================================

不要在代码里写死：

IP
域名
AstrBot 端口

统一使用配置。

例如：

VITE_API_BASE=/api

注意：

任何 VITE_ 开头变量都会进入浏览器，因此绝对不能放密钥。

服务器内部 AstrBot 地址如果涉及秘密配置，应由 Nginx 或后端保存。

==================================================
十三、实现顺序
==================================================

严格按照以下顺序工作：

Step 1

检测：

Node
npm
Docker
Nginx
AstrBot

Step 2

明确输出：

AstrBot 是否存在。

Step 3

如果存在：

确认实际 API。

Step 4

创建 Vue 项目。

Step 5

完成 MockChatService。

Step 6

完成基本聊天 UI。

Step 7

完成 Markdown。

Step 8

完成流式输出。

Step 9

如果 AstrBot 存在：

完成 AstrBotAdapter。

Step 10

完成 Nginx 示例配置。

Step 11

运行：

npm install
npm run build

修复所有 TypeScript / Vue 编译错误。

Step 12

如果环境允许：

实际运行开发服务器并测试。

==================================================
十四、不要做的事情
==================================================

不要：

1. 猜 AstrBot API。

2. 自动安装 AstrBot。

3. 把 Gemini Key 放前端。

4. 把 AstrBot 管理端口公开公网。

5. 一上来创建复杂数据库。

6. 引入 Kubernetes。

7. 引入 Redis，除非确实有需求。

8. 引入微服务。

9. 使用 WordPress。

10. 为了所谓“企业级架构”把一个简单个人项目复杂化。

这个项目优先：

简单
稳定
容易维护
方便部署

==================================================
十五、完成标准
==================================================

最终我应该能够：

npm install
npm run build

得到：

dist/

然后让 Nginx 托管 dist。

访问：

https://我的域名

即可看到聊天界面。

如果 AstrBot 可用：

网页可以实际向 AstrBot 发消息，并实时显示 Gemini 回复。

如果 AstrBot 不可用：

网页仍然可以通过 Mock 模式运行，同时明确提示尚未连接 AstrBot。

最后输出：

1. AstrBot 检测结果
2. 项目目录
3. 已实现功能
4. AstrBot API 对接方式
5. Nginx 部署方法
6. 需要我手动修改的配置
7. 当前仍存在的问题

所有涉及 AstrBot 的结论都必须基于实际环境检查，而不是猜测。